#include "adjacencyMatrix.h"
#include <stdio.h>

void FileRead(char *filename, cell_t *lista){
    while(lista!=NULL){
        /*ustala sciezke usuwajac niepotrzebne fragmenty listy*/
    }
    finalSolve(filename, lista);
}