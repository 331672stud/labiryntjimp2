


void StdRead(plik, char *filename, celltype *graf){
    FILE *plik=fopen(filename, "r");
    celltype **labirynt[m][n];
    while(/*plik!=EOF*/){
        /*assigns adjacency to labirynt*/
    }
    conv2graph(labirynt, graf);
    //done
}